<div class="container">
  <div class="jumbotron">
    <h1>Bustle</h1>
    {{admin-button article=article}}
  </div>
  <div class="well">
    {{#each model as |article|}}
    <h2>{{article.title}}</h2>
    <img src={{article.image}} alt={{article.title}}>
      <p>
        by <strong>{{article.author}}</strong>
      </p>
      {{article-tile article=article}}
      <hr>
    {{/each}}
  </div>
  <div class="well">
  </div>
</div>
